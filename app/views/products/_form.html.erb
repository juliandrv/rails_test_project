<div class="container">
	<div class="row">
		<div class="col"></div>
		<div class="col">
			<%= form_for @product do |f| %>
					<div class="form-group">
						<%= f.label "Product Name" %>
						<%= f.text_field :name, class: "form-control", placeholder: "Ingrese el nombre del producto" %>
					</div>
					<div class="form-group">
						<%= f.label "Price" %>
						<%= f.number_field :price, class: "form-control", step: "0.01", placeholder: "Ingrese el precio del producto" %>
					</div>
					<div class="form-group">
						<%= f.label "Description" %>
						<%= f.text_area :description, class: "form-control", placeholder: "Ingrese la descripción del producto" %>
					</div>
					<div class="form-group">
						<%= f.submit "#{name_button}", class: "btn btn-danger text-white font-weight-bold" %>
					</div>
			<% end %>
		</div>
	<div class="col"></div>
	</div>
</div>